/**
 * Created by Administrator on 2017/2/16.
 */
define('ck_table', ['jquery'], function ($) {
    // var TABLE='ck-table'
    /*var talbe = function (options) {
        var that=this;
        that.OPTIONS={
            caption:'',//标题

        };
        that.init=function(options){
            $.extend(that.OPTIONS,options||{});
        };
        that.init(options);
    };*/

});